<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".share" />
        <activity android:name=".HistoryLayout" />
        <activity android:name=".CaseHistory" />
        <activity
            android:name=".Reminder"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".EditCaseDetail" />
        <activity android:name=".ShowCaseDetail" />
        <activity
            android:name=".dialog"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Register" />
        <activity android:name=".listview_layout" />
        <activity android:name=".addCase" />
        <activity android:name=".Calendar" />
        <activity android:name=".showCase" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>